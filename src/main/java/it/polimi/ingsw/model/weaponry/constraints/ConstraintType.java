package it.polimi.ingsw.model.weaponry.constraints;

import it.polimi.ingsw.model.player.Activity;

/**
 * An enumeration containing the four possible {@link Activity} types.
 */
public enum ConstraintType {
    ALIGNMENT,
    DISTANCE,
    IDENTITY,
    ORDER,
    ROOM,
    VISIBILITY
}